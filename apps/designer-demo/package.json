{
  "name": "@web-scada/designer-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@web-scada/core": "workspace:*",
    "@web-scada/designer-engine": "workspace:*",
    "@web-scada/geometry": "workspace:*",
    "@web-scada/renderer-svg": "workspace:*",
    "@web-scada/symbols": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  }
}
