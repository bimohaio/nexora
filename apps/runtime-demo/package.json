{
  "name": "@web-scada/runtime-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:integrated": "vite --host 127.0.0.1 --port 4173 --strictPort",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@web-scada/core": "workspace:*",
    "@web-scada/datasource-core": "workspace:*",
    "@web-scada/datasource-simulator": "workspace:*",
    "@web-scada/designer-engine": "workspace:*",
    "@web-scada/renderer-svg": "workspace:*",
    "@web-scada/symbols": "workspace:*",
    "@web-scada/runtime-engine": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  }
}
