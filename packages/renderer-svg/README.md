# @web-scada/renderer-svg

Readonly SVG rendering boundaries and incremental change-set contracts. Phase 0 intentionally provides no renderer implementation; future renderers must never mutate a document.
