# @web-scada/core

Environment-neutral document, validation, serialization, migration, event, command, and error contracts. It intentionally imports no DOM, renderer, designer, runtime, or transport API.
