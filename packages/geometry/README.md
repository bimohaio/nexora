# @web-scada/geometry

DOM-independent geometry contracts and pure coordinate, rotation, scale, viewport, rectangle, bounds, intersection, route-input, dimension, and grid utilities.

Viewport convention: `screen = canvas × zoom + translation`. Port transforms apply normalized local position, center-relative scale, center-relative rotation, then canvas translation.
