# @web-scada/web-components

Future custom-element adapters for designer and runtime engines. Components will dispatch localized browser events and use CSS theme tokens, while domain state remains owned by engines. No production UI is implemented in Phase 0.
