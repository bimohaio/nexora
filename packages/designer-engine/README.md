# @web-scada/designer-engine

Public boundaries for future selection, viewport, commands, input, clipboard, and editing interactions. Continuous gestures such as dragging will commit one final history command, not one command per pointer event.
