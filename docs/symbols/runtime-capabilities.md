# Runtime capabilities

Definitions declare generic capabilities and supported runtime fields. Runtime and Binding engines
remain symbol-neutral; they resolve generic fields such as state, fill, text, value, level and
direction. Capabilities describe support but do not subscribe to tags or evaluate unrestricted
expressions. `animation-compatible` and `alarm-visual-compatible` are future-facing declarations,
not Phase 10 implementations.
