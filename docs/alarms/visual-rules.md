# Alarm Visual Rules

Rules use emphasis, overlay, indicator and semantic token identifiers. They may
reference an animation definition, never instantiate it. SVG structures and theme
token mapping remain renderer responsibilities.

Acknowledged alarms can preserve, de-emphasize or become static. Reduced motion
removes animation references while retaining static cues.
