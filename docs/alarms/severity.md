# Alarm Severity

Built-ins are information (10), warning (20), alarm (30) and critical (40).
Identifiers and ranks are semantic and contain no colors. Unknown severities fail
validation. Custom definitions live in an isolated registry instance.

Multi-alarm priority is lifecycle, severity rank, acknowledgment, shelving,
activation time and finally alarm ID.
