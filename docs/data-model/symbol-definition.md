# Symbol definition

Symbols are metadata-driven: stable type, localization key, category, default/minimum size, ports, editable/bindable property metadata, supported states, and renderer ID. `SymbolRegistry` resolves definitions independently of frameworks; Phase 0 includes a deterministic in-memory registry but no industrial symbol library.
