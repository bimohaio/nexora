# Symbol definition

Symbols are metadata-driven: stable type, localization and description keys, extensible category, default/minimum size, ports, editable/bindable property metadata, states, and extensions. `InMemorySymbolRegistry` validates dimensions and ports, rejects duplicates unless replacement is explicit, and returns snapshot collections. Phase 1 includes Rectangle and Text metadata only—no renderer.
