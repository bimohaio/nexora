# Runtime settings

Runtime settings contain refresh interval, optional staleness interval, default quality, timezone, and locale. They configure future runtime behavior but never contain current tag values or provider connections.
