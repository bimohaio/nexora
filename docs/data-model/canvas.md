# Canvas

Canvas width, height, and grid size are positive logical values. Background is a color or `transparent`; units are metadata. The viewport convention is `screen = canvas × zoom + translation`, with finite translation and positive zoom.
