# Node

A node references a stable symbol type and layer. Its transform stores logical x/y, positive dimensions, quarter-turn rotation, and x/y scale. Properties are JSON-safe design values. Binding IDs reference document bindings. Optional `parentId` forms grouping; semantic validation must reject missing parents and cycles.
