# Variable

Variables are static design/configuration values with stable IDs, unique names, declared types, optional default/current configuration values, unit, description, and readonly status. Supported types are boolean, integer, number, string, color, date-time, and JSON. Live telemetry belongs in runtime state.
