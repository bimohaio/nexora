# Error codes

Validation codes are stable UPPER_SNAKE_CASE identifiers, separate from human-readable messages. Issues also carry JSON-path-like paths, severity, and structured context. Codes must not be repurposed; add a new code when semantics differ.
