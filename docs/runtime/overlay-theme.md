# Overlay theme

Overlay tokens use names such as `overlay.process-warning.fill`, `overlay.acknowledged.border` and
`overlay.offline.mask`. Themes alias semantic tokens and never contain renderer code. Theme changes
reproject presentation snapshots without touching alarm evaluation or severity resolution.
