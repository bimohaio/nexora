# Acknowledgement overlays

`resolveAcknowledgementOverlay` converts an existing `AlarmPresentation` into semantic badge,
ribbon, border, corner-marker, tooltip and optional pulse requests. It never acknowledges an alarm
or evaluates alarm state. Reduced motion removes pulse while retaining all static cues.
