# Accessibility motion

Reduced, minimal, static and accessibility policies replace motion with static border, solid
overlay, contrast border, priority badge, status ribbon and label highlight. Critical and emergency
alarms always preserve color-independent badge, shape, label and icon cues even when all animation
is paused.
