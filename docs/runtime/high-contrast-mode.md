# High contrast mode

Contrast policies contain semantic tokens including `contrast.warning`, `contrast.critical`,
`contrast.offline`, `contrast.communication` and `contrast.ack`. High/forced contrast requests
shape, pattern and badge indicators and never depends on color alone. Renderers map tokens to
platform resources.
