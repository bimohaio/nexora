# Alarm badge system

Supported badge requests are none, circle, triangle, diamond, square, octagon, numeric, stacked,
count and priority. One alarm uses a severity-appropriate shape; multiple alarms use a count badge.
Badge position and drawing belong to the renderer.

Every badge carries a semantic token. Count badges additionally expose numeric count and accessible
label data. Badge resolution is pure and stable for the same aggregate and theme.
